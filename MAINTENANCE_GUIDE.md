# Maintenance Guide

## Regular Tasks
- Regenerate content after structural changes.
- Run `python project_scripts/validate_course_structure.py`.
- Keep docs in `docs/course/` and `docs/maintenance/` aligned with code.

## Automation Scripts
See `project_scripts/` for generators and validators.
