# Project Templates Guide

This guide describes how to work with templates in `06-projects/`.

- Each project has `README.md`, `src/`, `tests/`, `solutions/`, and `requirements.txt`.
- Start by reading the README, then edit `src/main.py`.
- Add tests under `tests/` and run them regularly.
