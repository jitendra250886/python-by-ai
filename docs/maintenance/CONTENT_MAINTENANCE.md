# Content Maintenance

## Updating Notebooks
- Use `project_scripts/update_content.py <section>` to regenerate content.
- Re-run validation with `python project_scripts/validate_course_structure.py`.

## When to Regenerate
- After changing INDEX files
- After modifying generator scripts
- After adding new lessons or projects
